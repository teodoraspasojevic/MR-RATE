"""Model loading. One module per model family; each is the only place that family's code is used.

    nvidia.py                   loads the pretrained autoencoder + diffusion UNet
    report_conditioned_unet.py  that same diffusion UNet plus report cross-attention
"""
